import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:reelify/main.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Basic smoke test - widget should build
    expect(true, isTrue); // Placeholder until Firebase is initialized in tests
  });
}
